<%= form_with(model:[item, comment],url:item_comments_path(item),method: :post,remote: true) do |f| %>
  <%= f.text_area :comment, rows:'5',placeholder: "コメントをここに" %>
  <%= f.submit "送信する" %>
<% end %>